# openEyes 👁️

一个互联网信息收集与整合平台，通过网页（支持移动端）每日整合展示来自多个信息源的内容，并使用 AI 进行智能总结。

## 🛠️ 技术架构

### 后端
- **语言**: TypeScript
- **框架**: Express.js
- **数据库**: Prisma + SQLite
- **信息抓取**: RSS Parser + Cheerio + Axios
- **AI**: OpenAI API
- **CLI 工具**: Commander.js

### 前端
- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **API 调用**: 自定义 API 客户端

## 📦 安装

### 后端

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 初始化数据库
npx prisma generate
npx prisma db push

# 配置环境变量
cp .env .env.local
# 编辑 .env.local 填入你的 OPENAI_API_KEY
```

### 前端

```bash
# 回到项目根目录
cd ..

# 安装依赖
npm install
```

## ⚙️ 配置

### 后端配置

在 `backend/.env` 文件中配置以下环境变量：

```env
# 数据库
DATABASE_URL="file:./dev.db"

# AI 服务
OPENAI_API_KEY="your-api-key"
OPENAI_BASE_URL=""  # 可选，用于自定义 API 地址

# 抓取配置
CRON_SCHEDULE="0 8 * * *"  # 每天早上 8 点
```

## 🚀 运行

### 1. 启动后端服务

```bash
# 进入后端目录
cd backend

# 启动 API 服务
npm run dev

# 或构建并运行生产版本
npm run build
npm start
```

后端服务将运行在 http://localhost:3001

### 2. 启动前端服务

```bash
# 回到项目根目录
cd ..

# 启动开发服务器
npm run dev

# 或构建并运行生产版本
npm run build
npm start
```

前端服务将运行在 http://localhost:3000

## 📖 使用说明

### 后端 CLI 工具

后端提供了一个 CLI 工具，用于执行信息抓取和其他管理操作：

```bash
# 进入后端目录
cd backend

# 抓取所有信息源的内容
npm run dev fetch

# 生成每日摘要
npm run dev summary

# 列出所有信息源
npm run dev list:sources

# 列出最近的文章
npm run dev list:articles -n 20
```

### 前端使用

1. **访问前端应用**：打开 http://localhost:3000
2. **管理信息源**：访问 `/sources` 页面添加和管理信息源
3. **查看文章**：在首页查看今日信息汇总，在 `/articles` 页面查看全部文章
4. **触发抓取**：点击首页的"立即抓取"按钮（实际抓取操作在后端执行）

## 📁 项目结构

```
openEyes/
├── backend/                # 后端代码
│   ├── src/
│   │   ├── cli/            # CLI 工具
│   │   ├── lib/            # 核心功能
│   │   │   ├── ai.ts       # AI 处理
│   │   │   ├── db.ts       # 数据库操作
│   │   │   └── fetcher.ts  # 信息抓取
│   │   └── index.ts        # API 服务
│   ├── prisma/             # 数据库模型
│   └── .env                # 环境变量
├── src/                    # 前端代码
│   ├── app/                # Next.js App Router
│   │   ├── page.tsx        # 首页
│   │   ├── sources/        # 信息源管理
│   │   ├── articles/       # 文章列表
│   │   └── api/            # API 路由（预留）
│   ├── components/         # React 组件
│   ├── lib/                # 工具库
│   │   └── api.ts          # API 客户端
│   └── types/              # TypeScript 类型
└── package.json            # 前端依赖
```

## 🗄️ 数据模型

### Source（信息源）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 唯一标识 |
| name | String | 信息源名称 |
| type | String | 类型（rss/crawler/manual/social） |
| url | String | 来源 URL |
| config | String? | JSON 配置 |
| active | Boolean | 是否启用 |

### Article（文章）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 唯一标识 |
| sourceId | String | 关联信息源 |
| sourceName | String | 信息源名称 |
| title | String | 标题 |
| content | String? | 内容 |
| summary | String? | AI 总结 |
| url | String? | 原文链接 |
| isRead | Boolean | 是否已读 |
| isFavorite | Boolean | 是否收藏 |

## 📝 开发计划

- [ ] 定时任务自动抓取
- [ ] 文章详情页
- [ ] 社交媒体源支持
- [ ] 导出功能（PDF/EPUB）
- [ ] 离线阅读支持
- [ ] 多用户支持

## 📄 License

ISC

## 👥 Contributors

- Yipeng Zou <403650485@qq.com>
- Trae AI Assistant

---

**openEyes** - 让信息触手可及 👁️